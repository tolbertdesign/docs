{
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt build && nuxt export",
    "preserve": "yarn generate",
    "serve": "nuxt serve"
  },
  "dependencies": {
    "nuxt": "latest"
  },
  "devDependencies": {
    "@nuxt/content": "^1.4.0",
    "@nuxtjs/tailwindcss": "^2.0.0"
  }
}
